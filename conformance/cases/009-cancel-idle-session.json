{
  "id": "acp.v1.session.cancel.idle",
  "profile": "acp-core-v1",
  "title": "Idle cancellation is sent without waiting for a response",
  "description": "Send a cancellation notification for a valid idle session; no separate acknowledgement is expected or observed.",
  "steps": [
    {
      "action": "new_session",
      "save_as": "session_id"
    },
    {
      "action": "cancel",
      "session": "$session_id"
    }
  ],
  "checks": [
    {
      "type": "saved_non_empty_string",
      "key": "session_id"
    }
  ],
  "timeouts": {
    "request_timeout_ms": 10000
  }
}
