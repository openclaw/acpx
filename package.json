{
  "name": "acpx",
  "version": "0.1.3",
  "description": "Headless CLI client for the Agent Client Protocol (ACP) — talk to coding agents from the command line",
  "keywords": [
    "acp",
    "agent-client-protocol",
    "ai",
    "claude-code",
    "cli",
    "codex",
    "coding-agent"
  ],
  "license": "MIT",
  "author": "OpenClaw Team <dev@openclaw.ai>",
  "repository": {
    "type": "git",
    "url": "https://github.com/openclaw/acpx"
  },
  "bin": {
    "acpx": "dist/cli.js"
  },
  "files": [
    "dist",
    "skills",
    "README.md",
    "LICENSE"
  ],
  "type": "module",
  "scripts": {
    "build": "tsdown src/cli.ts --format esm --dts --clean --platform node --target node18 --no-fixedExtension",
    "build:test": "node -e \"require('node:fs').rmSync('dist-test',{recursive:true,force:true})\" && tsc -p tsconfig.test.json",
    "dev": "tsx src/cli.ts",
    "format": "oxfmt --write",
    "format:check": "oxfmt --check",
    "format:diff": "oxfmt --write && git --no-pager diff",
    "lint": "oxlint --type-aware src && npm run lint:persisted-key-casing",
    "lint:fix": "oxlint --type-aware --fix src && npm run format",
    "lint:persisted-key-casing": "tsx scripts/lint-persisted-key-casing.ts",
    "precommit": "npm exec -- lint-staged && npm run -s build",
    "prepack": "npm run build",
    "prepare": "husky",
    "release": "release-it",
    "release:ci": "release-it --ci",
    "test": "npm run build:test && node --test dist-test/test/*.test.js",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@agentclientprotocol/sdk": "^0.14.1",
    "commander": "^13.0.0",
    "skillflag": "^0.1.4"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "husky": "^9.1.7",
    "lint-staged": "^16.2.7",
    "oxfmt": "^0.36.0",
    "oxlint": "^1.51.0",
    "oxlint-tsgolint": "^0.15.0",
    "release-it": "^19.2.4",
    "tsdown": "^0.21.0-beta.2",
    "tsx": "^4.0.0",
    "typescript": "^5.7.0"
  },
  "lint-staged": {
    "*.{js,ts}": [
      "oxfmt --write",
      "oxlint --fix"
    ],
    "*.{json,md}": [
      "oxfmt --write"
    ]
  },
  "engines": {
    "node": ">=18"
  }
}
